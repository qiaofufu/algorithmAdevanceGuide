#include <bits/stdc++.h>

using namespace std;

const int N = 1E5 + 10;

signed main() {
    cin >> n >> r;
    for(int i = 1; i <= n; i ++ ) cin >> a[i];
    for(int i = 1; i <= n - 1; i ++ ) cin >> x >> y;
}